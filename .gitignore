# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
.venv/
venv/
.env

# IDE / OS
.vscode/
.idea/
.DS_Store

# Logs and runtime
*.log
*.pid
.tmp/

# Kafka local data
kafka-data/
zookeeper-data/

# Build / test artifacts
.pytest_cache/
.coverage
htmlcov/

# Local video samples (often huge)
data/samples/*.mp4
data/samples/*.mkv
data/samples/*.mov

# Local Kafka overrides (optional copy of kafka.example.json)
config/kafka.json

# Local video ingest overrides (optional copy of video_ingest.example.json)
config/video_ingest.json

# Camera registry (your real camera definitions — use scripts/camera.py)
config/cameras.json
